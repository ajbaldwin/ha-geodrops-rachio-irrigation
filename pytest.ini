[pytest]
asyncio_mode = auto
pythonpath = .
# tests_brain runs natively with plugin autoload off (see docs/RELEASING.md), so
# pytest-asyncio is absent there and its option would warn as unknown.
filterwarnings =
    ignore:Unknown config option.*asyncio_mode:pytest.PytestConfigWarning
